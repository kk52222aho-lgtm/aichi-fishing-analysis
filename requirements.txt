# Core
requests>=2.31.0
beautifulsoup4>=4.12.0
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=14.0.0
python-dateutil>=2.8.2
astral>=3.2

# Statistical predictor
scikit-learn>=1.4.0
lightgbm>=4.3.0
joblib>=1.3.0

# LLM predictor
openai>=1.0.0
google-genai>=0.3.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# Dashboard (Streamlit Cloud / local)
streamlit>=1.30.0
plotly>=5.18.0
matplotlib>=3.8.0
japanize-matplotlib>=1.1.3

# Sheets / ingestion (optional but pulled by build_seed_dataset)
openpyxl>=3.1.0
gspread>=6.0.0
google-auth>=2.27.0

# YOLO (heavy — Streamlit Cloud のビルド失敗/OOM 回避のため無効化。
# アプリ起動・ダッシュボード・予測には不要。YOLO スクレイプを回す環境でだけ有効化する)
# ultralytics>=8.1.0
pillow>=10.2.0
